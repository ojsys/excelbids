# The application directory must never be served directly.
# This matters when the whole project is uploaded inside public_html.
Require all denied
<IfModule !mod_authz_core.c>
    Order allow,deny
    Deny from all
</IfModule>
